<div class="row">
  <div class="small-12 columns">
    <h1>Admins</h1>

    <ul>
      <% @admins.each do |admin| %>
        <li><%= link_to admin.name, admin_path(admin.id) %></li>
      <% end %>
    </ul>
  </div>
</div>
