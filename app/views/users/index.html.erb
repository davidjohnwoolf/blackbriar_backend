<div class="row">
  <div class="small-12 columns">
    <h1>Users</h1>
    <ul>
      <% @users.each do |user| %>
        <li><%= link_to user.name, user_path(user.id) %></li>
      <% end %>
    </ul>
  </div>
</div>
